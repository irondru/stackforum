div id="question-#{question.id}"
  = link_to question.title, question_path(question)
  = link_to ' Удалить', question_path(question), method: :delete, remote: true if question.user == current_user
  p